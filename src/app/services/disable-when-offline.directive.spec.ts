import { DisableWhenOfflineDirective } from './disable-when-offline.directive';

describe('DisableWhenOfflineDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableWhenOfflineDirective();
    expect(directive).toBeTruthy();
  });
});
